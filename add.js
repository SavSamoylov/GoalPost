"use strict";

var allValidation = function(name, email, password, confPass){

	if (name === " " || email === " " || password === " " || confPass === " "){
		throw new Error("Input is empty!");
	} else {
	return (" "); 
}
};
module.exports = allValidation;